apiVersion: v1
kind: Service
metadata:
    name: matsurigotopythonsamplevscodeflasktutorial
spec:
    type: LoadBalancer
    ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
    selector:
        app: matsurigotopythonsamplevscodeflasktutorial
